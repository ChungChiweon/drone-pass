import type {CanonicalShadowInput,CompatibilityResult} from "./shadow-pack-types";
const DIRECT=new Set(["DEFINITION","CLASSIFICATION","NUMERIC_THRESHOLD","TRUE_FALSE"]);const ADAPTER=new Set(["CONDITION_SELECTION","EXCEPTION_SELECTION","CASE_JUDGMENT"]);
export function analyzeShadowCompatibility(unit:CanonicalShadowInput):CompatibilityResult{if(unit.questionEligibility==="NOT_QUESTION_ELIGIBLE")return result("NOT_QUESTION_ELIGIBLE","Not question eligible",0);const supported=unit.supportedQuestionTypes??[];if(unit.questionEligibility==="COMPOSITE_ONLY")return result("COMPOSITE_SUPPORTED",undefined,1);if(supported.some(x=>DIRECT.has(x)))return result("DIRECTLY_COMPATIBLE",undefined,1);if(supported.some(x=>ADAPTER.has(x)))return result("COMPATIBLE_WITH_ADAPTER",undefined,.7);if(supported.length)return result("TEMPLATE_MISSING",`Missing template: ${supported.join(",")}`,0);return result("STRUCTURE_UNSUPPORTED","No supported question taxonomy",0);function result(compatibilityStatus:CompatibilityResult["compatibilityStatus"],blocker:string|undefined,expectedQuestionPotential:number):CompatibilityResult{return{knowledgeId:unit.knowledgeId,factType:unit.factType,questionEligibility:unit.questionEligibility,supportedQuestionTypes:supported,compatibilityStatus,requiredTemplate:blocker?.replace("Missing template: ",""),blocker,expectedQuestionPotential}}}
