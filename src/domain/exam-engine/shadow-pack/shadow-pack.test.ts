import {describe,expect,it} from "vitest";import {analyzeShadowCompatibility,buildLegalShadowPack,transformCanonicalToShadowFact,validateShadowPack,type CanonicalShadowInput} from ".";
const unit:CanonicalShadowInput={knowledgeId:"LKU-1",canonicalStatement:"25kg 초과인 경우 승인이 필요하다",subject:"비행장치",predicate:"승인 필요",object:"승인",factType:"NUMERIC_THRESHOLD",value:25,unit:"kg",operator:"GT",conditions:[{value:"사업용"}],exceptions:[{value:"군용은 제외"}],applicability:["초경량비행장치"],currentnessStatus:"CURRENT_EFFECTIVE",primarySourceId:"official-law",primarySourceLocator:"제1조",supportingSourceIds:[],supportingLocators:[],sourceCandidateIds:["C1"],topic:"flight-approval",subtopic:"approval",questionEligibility:"STANDALONE",supportedQuestionTypes:["NUMERIC_THRESHOLD","CASE_JUDGMENT"],consolidationConfidence:1,qualityScore:1,status:"CANONICAL_READY"};
describe("legal shadow pack",()=>{it("keeps operator, condition, exception and source trace",()=>{const fact=transformCanonicalToShadowFact(unit);expect(fact.operator).toBe("gt");expect(fact.conditions).toHaveLength(1);expect(fact.exceptions).toHaveLength(1);expect(fact.sourceReferences[0].locator).toBe("제1조");expect(fact.status).toBe("SHADOW_APPROVED")});it("rejects warning input from the transformer",()=>expect(()=>transformCanonicalToShadowFact({...unit,status:"CANONICAL_READY_WITH_WARNING"})).toThrow("SHADOW_INPUT_NOT_READY"));it("builds deterministic isolated packs",()=>{const a=buildLegalShadowPack({units:[unit],canonicalSetId:"set",canonicalChecksum:"sha",createdAt:"fixed"}),b=buildLegalShadowPack({units:[unit],canonicalSetId:"set",canonicalChecksum:"sha",createdAt:"fixed"});expect(a.checksum).toBe(b.checksum);expect(a.facts[0].id).toBe("SHF-LKU-1");expect(validateShadowPack(a).valid).toBe(true)});it("classifies compiler compatibility",()=>expect(analyzeShadowCompatibility(unit).compatibilityStatus).toBe("DIRECTLY_COMPATIBLE"))});
