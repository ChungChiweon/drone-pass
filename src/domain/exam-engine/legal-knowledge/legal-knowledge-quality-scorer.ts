import type {CanonicalLegalKnowledgeUnit} from "./legal-knowledge-types";
export function scoreCanonicalKnowledge(unit:Pick<CanonicalLegalKnowledgeUnit,"primarySourceLocator"|"currentnessStatus"|"subject"|"predicate"|"examRelevance"|"value"|"unit"|"operator"|"warnings"|"blockers">){const source=unit.primarySourceLocator&&unit.primarySourceLocator!=="DOCUMENT"&&unit.currentnessStatus.startsWith("CURRENT")?1:.5;const legal=(unit.subject&&unit.predicate)?1:.5;const numeric=unit.value==null?1:(unit.unit&&unit.operator&&unit.operator!=="NONE"?1:.4);const exam=unit.examRelevance==="HIGH"?1:unit.examRelevance==="MEDIUM"?.75:.2;const quality=Math.max(0,.3*source+.3*legal+.2*numeric+.2*exam-.05*unit.warnings.length);return{sourceProvenanceScore:source,legalCompletenessScore:Math.min(legal,numeric),examUsefulnessScore:exam,qualityScore:Number(quality.toFixed(4)),consolidationConfidence:Number(Math.min(source,legal,numeric).toFixed(4)),blocked:unit.blockers.length>0}}
