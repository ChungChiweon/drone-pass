import type {LegalCandidateInput,LegalRuleSignature} from "./legal-knowledge-types";
const UNIT:Record<string,string>={"킬로그램":"kg","킬로미터":"km","센티미터":"cm","미터":"m","시간":"h"};
export function normalizeLegalText(value:string|undefined){let text=(value??"").normalize("NFKC").toLowerCase();for(const [from,to] of Object.entries(UNIT))text=text.replaceAll(from,to);return text.replace(/[「」『』“”'"(),.·ㆍ\s]/g,"").replace(/(?:법률|대통령령|국토교통부령)에서정하는/g,"정하는");}
const join=(values:string[]|undefined)=>[...new Set((values??[]).map(normalizeLegalText).filter(Boolean))].sort().join("&");
export function createLegalRuleSignature(candidate:LegalCandidateInput):LegalRuleSignature{const parts={subject:normalizeLegalText(candidate.subject),predicate:normalizeLegalText(candidate.predicate),factType:candidate.factType??"UNKNOWN",value:candidate.value==null?"":String(candidate.value).replaceAll(",",""),unit:normalizeLegalText(candidate.unit??""),operator:candidate.operator??"NONE",applicabilitySignature:join(candidate.applicability),conditionSignature:join(candidate.conditions),exceptionSignature:join(candidate.exceptions)};return{candidateId:candidate.candidateId,signature:Object.values(parts).join("|"),...parts};}
