import type { LegalCandidateInput, RuleScore } from "./legal-validation-types";
const CUES=["다음 각 호의 어느 하나","각각","총","해당하는 경우","다음 각 목","국토교통부령으로 정하는","별표에서 정하는"];
export function validateLegalCondition(c:LegalCandidateInput):RuleScore{const raw=c.rawEvidenceText??"",joined=[...(c.conditions??[]),...(c.applicability??[])].join(" ");const passed:string[]=[],failed:string[]=[],warnings:string[]=[],blockers:string[]=[],evidence:string[]=[];for(const cue of CUES.filter(x=>raw.includes(x))){if(joined.includes(cue)||c.normalizedStatement?.includes(cue))passed.push(`CONDITION_${cue}`);else{failed.push(`CONDITION_${cue}`);blockers.push("CONDITION_SCOPE_MISSING");}}if(!failed.length)passed.push("CONDITION_SCOPE_PRESERVED");evidence.push(joined.slice(0,240));return{score:Math.max(0,1-blockers.length*.35),passedRules:passed,failedRules:failed,warnings,blockers,evidence};}
