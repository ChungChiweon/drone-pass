import type {TechnicalSourceCandidate,TechnicalSourceScore} from "./source-gap-acquisition-types";
export function scoreTechnicalSource(s:TechnicalSourceCandidate):TechnicalSourceScore{const authority=s.authority==="OFFICIAL_PRIMARY"?1:s.authority==="OFFICIAL_SECONDARY"?.9:s.authority==="PUBLIC_RESEARCH"?.7:.2;const score={authority,directUasRelevance:s.uasSpecific?1:.35,domesticExamRelevance:s.domestic?1:.25,technicalSpecificity:s.sourceType==="OTHER_PUBLIC"?.3:.85,currentness:s.currentness==="CURRENT"?1:s.currentness==="CONCEPT_STABLE"?.85:.45,topicBreadth:Math.min(1,s.topicCoverage.length/20),diagramUsefulness:.5,formulaUsefulness:.4,operationalUsefulness:s.uasSpecific?.8:.4,duplicationPenalty:0,total:0};score.total=Number(((score.authority+score.directUasRelevance+score.domesticExamRelevance+score.technicalSpecificity+score.currentness+score.topicBreadth+score.diagramUsefulness+score.formulaUsefulness+score.operationalUsefulness)/9-score.duplicationPenalty).toFixed(3));return score}
export function classifyPriority(s:TechnicalSourceCandidate){const score=scoreTechnicalSource(s);return s.domestic&&s.uasSpecific?"P0":s.uasSpecific?"P1":score.total>=.6?"P2":"P3"}
