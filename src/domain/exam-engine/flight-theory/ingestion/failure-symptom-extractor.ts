import type {EmergencySourceReference,FailureSymptom} from "./emergency-ingestion-types";
export function extractFailureSymptom(input:{symptomId:string;failureId:string;observedCondition:string;indicator?:string;detectionContext?:string;evidence:string;sourceReference?:EmergencySourceReference}):FailureSymptom|null{if(!input.failureId||!input.observedCondition.trim()||!input.evidence.trim()||!input.sourceReference)return null;return {symptomId:input.symptomId,failureId:input.failureId,observedCondition:input.observedCondition,indicator:input.indicator?.trim()??"",detectionContext:input.detectionContext?.trim()??"",sourceReference:input.sourceReference};}
