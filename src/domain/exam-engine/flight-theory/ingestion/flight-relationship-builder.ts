import type {FlightRelationship,FlightRelationshipType} from "../knowledge";import type {FlightExtractionContext} from "./flight-ingestion-types";
export function buildEvidenceRelationship(id:string,fromId:string,toId:string,relationType:FlightRelationshipType,context:FlightExtractionContext):FlightRelationship&{evidence:string;confidence:number}{if(!context.text.trim())throw new Error("RELATIONSHIP_EVIDENCE_REQUIRED");return {relationId:`flight-relation:${id}`,fromId,toId,relationType,sourceReferences:[{sourceId:context.sourceId,page:context.page,section:context.section}],evidence:context.text,confidence:.78}}
