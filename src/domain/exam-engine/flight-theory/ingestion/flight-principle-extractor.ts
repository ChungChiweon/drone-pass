import type {FlightPrinciple} from "../knowledge";import type {FlightExtractionContext} from "./flight-ingestion-types";
export function extractFlightPrinciple(id:string,name:string,relatedConceptIds:string[],context:FlightExtractionContext):FlightPrinciple&{rawEvidenceText:string;confidence:number}{return {principleId:`flight-principle:${id}`,name,inputs:[],mechanism:context.text,outputs:[],conditions:[],limitations:[],formulas:[],relatedConceptIds,sourceReferences:[{sourceId:context.sourceId,page:context.page,section:context.section}],rawEvidenceText:context.text,confidence:.76}}
