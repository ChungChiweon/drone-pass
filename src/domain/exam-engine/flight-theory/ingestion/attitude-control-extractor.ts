import type {FlightConcept,FlightSourceReference} from "../knowledge";
export function extractAttitudeControl(input:{conceptId:string;name:string;controlAxis:string;evidence:string;sourceReference:FlightSourceReference;confidence:number}):FlightConcept|null{if(!input.evidence.trim())return null;return {conceptId:input.conceptId,name:input.name,definition:input.evidence,properties:[`controlAxis:${input.controlAxis}`],variables:[],units:[],examples:[],misconceptions:[],topic:`flight:${input.controlAxis.toLowerCase()}`,sourceReferences:[input.sourceReference],confidence:input.confidence};}
