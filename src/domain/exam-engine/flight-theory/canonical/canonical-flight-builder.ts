import {createHash} from "node:crypto";import type {CanonicalFlight004GSet,CanonicalFlight004HSet,CanonicalFlightKnowledgeSet,CanonicalFlightUnit} from "./canonical-flight-types";
export function buildCanonicalFlight004A(input:Omit<CanonicalFlightKnowledgeSet,"setId"|"checksum">):CanonicalFlightKnowledgeSet{const core={...input,setId:"canonical-flight-theory:004a:v1"};return {...core,checksum:`sha256-${createHash("sha256").update(JSON.stringify(core)).digest("hex")}`}}
export function buildCanonicalFlight004B(input:Omit<CanonicalFlightKnowledgeSet,"setId"|"checksum">):CanonicalFlightKnowledgeSet{const core={...input,setId:"canonical-flight-theory:004b:v1"};return {...core,checksum:`sha256-${createHash("sha256").update(JSON.stringify(core)).digest("hex")}`}}
export function buildCanonicalFlight004F(input:Omit<CanonicalFlightKnowledgeSet,"setId"|"checksum">):CanonicalFlightKnowledgeSet{const core={...input,setId:"canonical-flight-theory:004f:v1"};return {...core,checksum:`sha256-${createHash("sha256").update(JSON.stringify(core)).digest("hex")}`}}
export function buildCanonicalFlight004G(input:Omit<CanonicalFlight004GSet,"setId"|"checksum">):CanonicalFlight004GSet{const core={...input,setId:"canonical-flight-theory:004g:v1" as const};const checksumInput={...core,generatedAt:undefined};return {...core,checksum:`sha256-${createHash("sha256").update(JSON.stringify(checksumInput)).digest("hex")}`}}
export function buildCanonicalFlight004H(input:Omit<CanonicalFlight004HSet,"setId"|"checksum">):CanonicalFlight004HSet{const core={...input,setId:"canonical-flight-theory:004h:v1" as const};const checksumInput={...core,generatedAt:undefined};return {...core,checksum:`sha256-${createHash("sha256").update(JSON.stringify(checksumInput)).digest("hex")}`}}
export function supportedTypes(unit:CanonicalFlightUnit){return unit.supportedQuestionTypes}
