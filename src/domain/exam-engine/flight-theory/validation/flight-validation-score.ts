import type {FlightValidationInput,FlightValidationResult} from "./flight-validation-types";
export function scoreFlightValidation(input:FlightValidationInput):FlightValidationResult{const source=input.sourceReferences.length&&input.rawEvidenceText?.trim()?1:0;const structure=input.structureComplete?1:.5;const science=input.scientificallyConsistent?1:0;const relation=input.relationshipConsistent===false?0:1;const visual=input.visualSupport==="REQUIRED"?.5:1;const score=source*.3+structure*.25+science*.25+relation*.1+visual*.05+.05;const blockers=[...(input.blockers??[])];const status:FlightValidationResult["validationStatus"]=blockers.length?(input.knowledgeType==="FORMULA"?"BLOCKED_FORMULA":input.knowledgeType==="RELATIONSHIP"?"BLOCKED_RELATIONSHIP":"BLOCKED_STRUCTURE"):score>=.95?(input.warnings?.length?"VALIDATED_WITH_WARNING":"VALIDATED"):score>=.9?"VALIDATED_WITH_WARNING":"REVIEW_REQUIRED";return {knowledgeId:input.knowledgeId,knowledgeType:input.knowledgeType,topicId:input.topicId,validationStatus:status,validationScore:Number(score.toFixed(3)),sourceEvidenceScore:source,structureScore:structure,scientificConsistencyScore:science,relationshipScore:relation,visualSupportScore:visual,warnings:input.warnings??[],blockers,evidence:input.rawEvidenceText??"",recommendedAction:status.startsWith("VALIDATED")?"CANONICAL_CANDIDATE":"REVIEW"}}
