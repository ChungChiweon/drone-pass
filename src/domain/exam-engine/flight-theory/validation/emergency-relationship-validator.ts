import {emergencyResult,hasPrimaryEvidence,type SourceBackedInput} from "./emergency-validation-types";
const allowedTargets:Record<string,Set<string>>={TRIGGERS:new Set(["EMERGENCY_DECISION"]),DETECTED_BY:new Set(["FAILURE_SYMPTOM"]),CAUSES:new Set(["FAILURE_MODE"]),RESULTS_IN:new Set(["FAILURE_MODE","FAILURE_SYMPTOM"]),REQUIRES:new Set(["EMERGENCY_PROCEDURE","EMERGENCY_DECISION"]),PREVENTED_BY:new Set(["SAFETY_KNOWLEDGE"]),AFFECTS:new Set(["FAILURE_MODE","SAFETY_KNOWLEDGE"]),PART_OF:new Set(["FAILURE_MODE","EMERGENCY_PROCEDURE"]),PRECEDES:new Set(["EMERGENCY_PROCEDURE"]),FOLLOWED_BY:new Set(["EMERGENCY_PROCEDURE"]),CONTRASTS_WITH:new Set(["FAILURE_MODE","EMERGENCY_DECISION"])};
export function validateEmergencyRelationship(x:SourceBackedInput&{relationId:string;sourceKnowledgeId:string;targetKnowledgeId:string;relationType:string},types:Map<string,string>){const sourceValid=hasPrimaryEvidence(x);const endpoints=types.has(x.sourceKnowledgeId)&&types.has(x.targetKnowledgeId)&&x.sourceKnowledgeId!==x.targetKnowledgeId;const semantic=Boolean(allowedTargets[x.relationType]?.has(types.get(x.targetKnowledgeId)??""));return emergencyResult({knowledgeId:x.relationId,knowledgeType:"RELATIONSHIP",sourceValid,structureValid:endpoints,boundaryValid:semantic,blockers:[...(!sourceValid?["PRIMARY_SOURCE_OR_LOCATOR_MISSING"]:[]),...(!endpoints?["RELATIONSHIP_ENDPOINT_INVALID"]:[]),...(!semantic?["RELATIONSHIP_DIRECTION_OR_TARGET_TYPE_INVALID"]:[])],reason:semantic?"Direction and target type match relation semantics.":"Co-mention does not justify this relation direction or target type."});}
