import type {Flight004BValidationInput,Flight004BValidationResult,Flight004BValidationStatus} from "./flight-004b-validation-types";
export function scoreFlight004BValidation(input:Flight004BValidationInput):Flight004BValidationResult{const source=input.sourceReferences.length>0&&input.rawEvidenceText.trim()?1:0;const relation=input.relationshipEvidence??1;const visual=input.visualSupport??1;const score=source*.35+input.structureCompleteness*.25+input.conceptClarity*.2+relation*.1+visual*.1;const blockers=[...(input.blockers??[])];if(!source)blockers.push("SOURCE_EVIDENCE_MISSING");let status:Flight004BValidationStatus;if(blockers.length)status=input.knowledgeType==="RELATIONSHIP"?"BLOCKED_RELATIONSHIP":input.knowledgeType==="VISUAL"?"BLOCKED_VISUAL":source?"BLOCKED_STRUCTURE":"BLOCKED_SOURCE";else status=score>=.95?(input.warnings?.length?"VALIDATED_WITH_WARNING":"VALIDATED"):"VALIDATED_WITH_WARNING";return {knowledgeId:input.knowledgeId,knowledgeType:input.knowledgeType,topicId:input.topicId,validationStatus:status,validationScore:Number(score.toFixed(3)),sourceEvidenceScore:source,structureCompletenessScore:input.structureCompleteness,conceptClarityScore:input.conceptClarity,relationshipEvidenceScore:relation,visualSupportScore:visual,warnings:input.warnings??[],blockers,evidence:input.rawEvidenceText,recommendedAction:status.startsWith("VALIDATED")?"CANONICAL_CANDIDATE":"REVIEW"};}
