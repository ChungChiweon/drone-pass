import type {FlightKnowledgeType,FlightValidationResult,FlightValidationStatus} from "./flight-validation-types";
export type OperationalValidationInput={knowledgeId:string;knowledgeType:FlightKnowledgeType;topicId:string;sourceReferences:unknown[];evidence:string;structureComplete:boolean;operationallyFaithful:boolean;orderingOrDecisionEvidence:boolean;relationshipConsistent:boolean;warnings?:string[];blocker?:FlightValidationStatus};
export type OperationalValidationResult=FlightValidationResult&{operationalFidelityScore:number;orderingDecisionScore:number};
export function validateOperationalInput(input:OperationalValidationInput):OperationalValidationResult{const sourceScore=input.sourceReferences.length>0&&input.evidence.trim().length>0?1:0;const structureScore=input.structureComplete?1:0;const operationalScore=input.operationallyFaithful?1:0;const orderingScore=input.orderingOrDecisionEvidence?1:0;const relationshipScore=input.relationshipConsistent?1:0;const validationScore=Number((sourceScore*.3+structureScore*.25+operationalScore*.2+orderingScore*.15+relationshipScore*.1).toFixed(3));const blocker=input.blocker??(sourceScore===0?"BLOCKED_SOURCE":undefined);const validationStatus:FlightValidationStatus=blocker??(validationScore>=.95?(input.warnings?.length?"VALIDATED_WITH_WARNING":"VALIDATED"):validationScore>=.9?"VALIDATED_WITH_WARNING":"REVIEW_REQUIRED");return {knowledgeId:input.knowledgeId,knowledgeType:input.knowledgeType,topicId:input.topicId,validationStatus,validationScore,sourceEvidenceScore:sourceScore,structureScore,scientificConsistencyScore:operationalScore,operationalFidelityScore:operationalScore,orderingDecisionScore:orderingScore,relationshipScore,visualSupportScore:1,warnings:input.warnings??[],blockers:blocker?[blocker]:[],evidence:input.evidence,recommendedAction:validationStatus.startsWith("VALIDATED")?"CANONICAL_CANDIDATE":"REVIEW"};}
