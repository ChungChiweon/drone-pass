import type{WeatherSourceMetadata}from"./weather-source-types";export class WeatherSourceRegistry{private readonly records=new Map<string,WeatherSourceMetadata>();register(source:WeatherSourceMetadata){if(this.records.has(source.sourceId))throw new Error("DUPLICATE_WEATHER_SOURCE");this.records.set(source.sourceId,structuredClone(source))}get(sourceId:string){const x=this.records.get(sourceId);return x?structuredClone(x):undefined}list(){return[...this.records.values()].map((source)=>structuredClone(source))}}
